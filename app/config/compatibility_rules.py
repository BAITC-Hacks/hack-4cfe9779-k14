"""Reserved for approved compatibility profiles supplied by the partner."""

# No category profile is enabled until the partner approves its mandatory and
# prohibited interchangeability parameters.
APPROVED_COMPATIBILITY_PROFILES: dict[str, dict[str, tuple[str, ...]]] = {}
